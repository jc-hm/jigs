// Shared Tailwind classes for the filled-output text block.
// Used in StreamingOutput (live) and Fill history (committed) so both
// render identically and changing the look only requires one edit here.
export const OUTPUT_TEXT = "whitespace-pre-wrap font-mono text-xs text-gray-800";
